#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME}.Listeners;#end

import org.bukkit.event.Listener;
import org.bukkit.event.EventHandler;

public class ${NAME} implements Listener {
    
}
